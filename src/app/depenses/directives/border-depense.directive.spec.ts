import { BorderDepenseDirective } from './border-depense.directive';

describe('BorderDepenseDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderDepenseDirective();
    expect(directive).toBeTruthy();
  });
});

import { DepenseAmountColorPipe } from './depense-amount-color.pipe';

describe('DepenseAmountColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DepenseAmountColorPipe();
    expect(pipe).toBeTruthy();
  });
});
